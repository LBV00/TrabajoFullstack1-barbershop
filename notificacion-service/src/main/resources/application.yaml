server:
  port: 7099

spring:
  application:
    name: notificacion-service

  datasource:
    url: jdbc:mysql://localhost:3306/db_barbershop_notificaciones?createDatabaseIfNotExist=true
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.sql