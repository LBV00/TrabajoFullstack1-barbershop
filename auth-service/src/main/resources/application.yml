server:
  port: 7096

spring:
  application:
    name: auth-service
  datasource:
    url: jdbc:mysql://localhost:3306/db_barbershop_auth?createDatabaseIfNotExist=true
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog-master.sql

jwt:
  secret: barbershop-secret-key-2025-fullstack-duoc-uc-muy-segura
  expiration-ms: 28800000  # 8 horas
